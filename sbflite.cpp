#include <iostream>

/*
 * TODO:
 * - Prometheus metrics server. 
 *      Upon GET request -> Request data from inverters? (One data fetch might take a few seconds!)
 */
int main(int argc, char *argv[])
{
    // Create Prometheus listening server
    Prometheus p;
    return 0;
}